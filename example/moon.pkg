import {
  "lepus-apps/webview",
  "moonbitlang/async",
  "moonbitlang/async/fs" @fs,
  "moonbitlang/core/json",
}

supported_targets = "+native"

options(
  is_main: true,
  link: {
    "native": {
      "cc-link-flags": "-lc++ -lobjc -framework Cocoa -framework WebKit",
    },
  },
)
