import {
  "lepus-apps/webview",
  "moonbitlang/core/json",
}

supported_targets = "+native"

options(
  is_main: true,
  link: {
    "native": {
      // MacOS / Linux
      "cc-link-flags": "-Llib -lwebview -Wl,-rpath,lib",
      // Windows
      // "cc-link-flags": "lib/webview.lib",
    },
  },
)
