import {
  "moonbitlang/core/json",
  "moonbitlang/core/encoding/utf8" @encoding/utf8,
}

supported_targets = "+native"

options(
  "native-stub": [ "stub.c" ],
  targets: {
    "binding.mbt": [ "native" ],
    "command.mbt": [ "native" ],
    "plugin.mbt": [ "native" ],
    "webview.mbt": [ "native" ],
  },
)
